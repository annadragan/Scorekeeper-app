import styled from 'styled-components';

export default function Play({ secondpage }) {
  return <PlayPage>{secondpage}</PlayPage>;
}

const PlayPage = styled.section`
  // padding: 12px 20px;
  // display: flex;
  // justify-content: center;
  // font-size: 1.2rem;
`;
